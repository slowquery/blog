# K8s 앱 env override — deploy/.env.app 로 복사 (git 커밋 금지)
#
# infra(MONGO_URI, REDIS_*)는 deploy/scripts/generate-k8s-env.sh 가 조합합니다.
# 아래는 앱 전용 키만 덮어씁니다.

# 기존 config.js secret_key 유지 권장 (세션·admin HMAC 호환)
SECRET_KEY=bL0GSeCreTKeY@#$

# playstyle devtool Redis 비밀번호를 못 읽을 때만 설정
# REDIS_PASSWORD=
